<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.quynh.feedmebot">

    <uses-sdk tools:overrideLibrary="android.support.v18.leanback"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Student.LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Professor.ProfLogIn" />
        <activity android:name=".Profile" />
        <activity android:name=".EditProfile" />
        <activity android:name="Student.Survey" />
        <activity android:name=".CourseOverview" />
        <activity android:name=".AssignmentOverview" />
        <activity android:name="Professor.Statistics" />
        <activity android:name="Student.Predictor" />


    </application>

</manifest>